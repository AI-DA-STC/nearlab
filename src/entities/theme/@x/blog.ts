/**
 * Cross-import surface for `entities/blog`. Same reasoning as `@x/paper.ts`:
 * a blog is filed under a research theme, and the seam is type-only.
 */
export type { ThemeId } from '../model/theme';
